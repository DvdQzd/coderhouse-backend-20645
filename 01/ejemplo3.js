const texto = 'hola ';
texto = texto + 'mundo';

console.log(texto)

const alumno = {nombre: 'Juan'}
alumno.nombre = 'Pedro'

console.log(alumno);

const arr = [1, 2, 3, 4];
arr.push(5)
arr.pop()

console.log(arr);

const push = 'a';
console.log(push)